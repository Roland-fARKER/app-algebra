import { EquationFormatPipe } from './equation-format.pipe';

describe('EquationFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new EquationFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
